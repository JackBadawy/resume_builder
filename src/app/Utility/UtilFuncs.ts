//type classes = {};

/* export const getCoordinateClass = (
  value: "10" | "70",
  type: "top" | "left"
) => {
  const classes: { [key in "10" | "70"]: string } = {
    "10": "10",
    "70": "70",
  };

  const className = `${type}-${classes[value]}`;
  console.log(`Generated class for ${type}:`, className); // Log the generated class name
  return className;
};
 */
